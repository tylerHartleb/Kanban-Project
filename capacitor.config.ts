import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SENG-513-PROJECT',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
